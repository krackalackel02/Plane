// Matches the CSS `glitch-out` collapse keyframes in welcomeOverlay.css -
// popups stay mounted this long after a dismiss so the exit animation
// (collapsing into a line of light) gets to finish playing before unmount.
export const GLITCH_EXIT_MS = 420;
